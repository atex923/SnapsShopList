# SnapsShopList V0.0.7 驗證紀錄

驗證日期：2026-09-06

## 已完成

- 商品照片控制列無黑色底板且位於照片外；掃碼頁的採買記事入口已通過實機編譯。
- 待買清單單行名稱／省略條碼／垃圾桶、條碼 popover、刪除確認及隱藏數量控制已通過實機編譯。
- 首頁掃描方格使用米藍色，兩個首頁方格的副文字均已移除。
- 本次更新已成功安裝至 Atex-iPhone16；手機處於鎖定狀態，因此命令列自動啟動仍由 iOS 拒絕。
- 首頁價格摘要確認僅顯示最低與最高價格，無平均價格。
- 掃碼既有商品頁已完成照片切換／更換／確認刪除、時間店家價格表及新增資料導頁。
- 採買記事摘要、商品紀錄三種排序、詳細資料展開及底部收合式價格記憶助手已通過 Swift 實機編譯。
- 空白舊照片檔名的刪除安全防護已加入。
- 本次 UI 更新的 Xcode Analyze：成功。
- 本次 UI 更新已安裝到 Atex-iPhone16；安裝後手機處於鎖定狀態，系統拒絕命令列自動啟動，需解鎖後由桌面開啟確認互動。
- 中文 App 名稱及首頁標題已統一為「購物記本」；Xcode 專案、Target、執行檔與輸出檔使用 `SnapsShopList`。
- AppIcon 已加入購物車結合相機的 1024 × 1024 RGB PNG，並由 Xcode 資產編譯器驗證。
- iPhoneOS 26.5 SDK Debug 無簽署通用建置：成功。
- Xcode Analyze 靜態分析：成功。
- iPhoneOS 26.5 SDK Release／CloudKit 程式分支無簽署建置：成功。
- `PurchaseLogic.swift` 純邏輯 smoke tests：成功，涵蓋購買數量、買一送一、每 100 g、kg 換算、店家 key 與表單驗證。
- Atex-iPhone16（iPhone 16 / iPhone17,3）Automatic Signing Debug 建置：成功。
- Atex-iPhone16 安裝：成功。
- Atex-iPhone16 啟動：成功；啟動後再次查詢程序仍持續運行，未立即閃退。
- 相機返回流程改由父 sheet binding 控制，移除相機內可能誤關閉商品表單的環境 `dismiss()`。
- 相簿寫入時點改為拍照回傳後立即執行，並補上拒絕權限／寫入失敗提示。
- 商品照片為選填；零張照片時保存流程不執行影像工作，有照片處理錯誤時記入 Log 並繼續保存資料。
- 商品建檔相機改由表單根畫面的 `fullScreenCover` 管理，開啟期間鎖定父表單互動式關閉。
- 分店與容量選填驗證：通過；容量空白可保存為未填寫，非空白時仍必須大於 0。
- 商品相機、採買欄位及 GPS 縣市／行政區已按指定位置重新排列。
- iPhone 16 / iOS 26.5 模擬器首頁截圖：1179 × 2556；雙方格為等大正方形、三區塊不需捲動、頁尾與 Log 入口位於底部。
- 條碼掃描 `AVCaptureSession` 的初始化、設定、啟動與停止均不在主執行緒。
- 商品照片新資料僅寫入 SwiftData external storage；影像縮放／JPEG 編碼位於背景 Task。
- 舊照片整併先確認 `imageData` 完整並成功存檔，才清除重複本機 JPEG。
- 店家記憶與待買關聯刪除使用 SwiftData Predicate。
- V0.0.6 完整保存在 `history/V0.0.6/`。

## 仍需人工操作確認

- 實際拿食品條碼測試各支援格式的辨識速度。
- 實際拍攝一般與近拍照片、允許相簿權限，確認焦距與照片結果符合需求。
- 在 iCloud 已啟用的正式開發團隊下，以兩台裝置確認 CloudKit 資料與照片同步。

上述項目需要鏡頭前實物、手動點選權限或第二台 iCloud 裝置，無法只靠命令列自動完成。
